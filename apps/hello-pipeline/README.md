# hello-pipeline

Minimal file-based app that writes text, then reads it back through blocks.

Run:

```bash
cargo run -p blocks-cli -- compose run blocks apps/hello-pipeline/app.yaml apps/hello-pipeline/input.example.json
```

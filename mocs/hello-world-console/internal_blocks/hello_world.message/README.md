# hello_world.message

Private block descriptor example for `hello-world-console`.

This block stays inside the moc and is not registered in the global `blocks/` registry.

The current MVP keeps the actual hello-world behavior in `moc.main`, so this internal block is a structural example rather than a wired runtime dependency.

# hello-pipeline frontend

This directory is reserved for the future Tauri + TypeScript launcher.

Frontend launchers should only compose frontend-capable blocks and call into the shared Rust contract, registry, and runtime layers.

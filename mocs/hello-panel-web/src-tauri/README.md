# src-tauri

This directory marks the Tauri host boundary for `hello-panel-web`.

The current MVP now includes a static browser preview in `../preview/index.html`, but it still does not ship a real Tauri host shell. A future iteration can add that shell here without changing the `moc` shape.

# core.llm.chat

Minimal MVP chat block.

This block now also ships as a standalone Rust crate at `blocks/core.llm.chat/rust/`.

Current runner behavior is a deterministic local mock so the flow remains testable without external model dependencies.

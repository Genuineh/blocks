# core.json.transform

Returns the provided `source` object as `result`. This is a minimal placeholder transform for MVP.

This block now also ships as a standalone Rust crate at `blocks/core.json.transform/rust/`.

# core.http.get

Performs a minimal GET request.

This block now also ships as a standalone Rust crate at `blocks/core.http.get/rust/`.

Current MVP runner support:

- plain `http://` URLs
- no HTTPS yet
- response body returned as UTF-8 text

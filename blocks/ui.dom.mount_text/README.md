# ui.dom.mount_text

Minimal frontend block.

It mounts the provided `text` into a target DOM element and returns `mounted: true`.

`block.yaml` remains the AI-facing contract, while the actual frontend behavior lives in `tauri_ts/src/index.ts`.

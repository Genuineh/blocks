# ui.counter.mount

Minimal interactive frontend block.

It mounts a counter UI into a target DOM element and returns `mounted: true`.

`block.yaml` remains the AI-facing contract, while the real frontend behavior lives in `tauri_ts/src/index.ts`.
